export class Repos {
    constructor(public id: number, public fullName:string, public description: string,public language: string, public date: Date, public github: string, public live:string){

    }
}
